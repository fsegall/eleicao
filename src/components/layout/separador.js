import React from 'react';
import styled from 'styled-components';

// Linha Separadora de Layout
export const Separador = styled.hr`
  margin: 2rem 0 2rem 0;
  background-color: #ccc;
  height: 0.5rem;
  border: none;
`;